import InterruptHandler from './InterruptHandler';

class Shell extends InterruptHandler {

	constructor(os) {
		super(os);
	}

	input(e) {
		// Handle input
	}

	start() {
		// Start process
	}
}

export default Shell;
